# S20.4 Diff 红绿着色

日期：2026-05-15
负责人：dev3
优先级：🟡 P1
估时：3h

## 问题

当前代码变更预览是纯文本代码块，无法区分新增行和删除行。用户必须逐行阅读才能理解变更。

## 实现

### 文件: `src/cli/diff-renderer.ts` (新建)

```typescript
// 解析 unified diff 格式
function parseDiff(diffText: string): DiffFile[] {
  // 解析 @@ -a,b +c,d @@ 头部
  // 提取每个文件的 hunks
}

interface DiffFile {
  file: string;
  hunks: DiffHunk[];
}

interface DiffHunk {
  oldStart: number; newStart: number;
  lines: DiffLine[];
}

interface DiffLine {
  type: 'context' | 'added' | 'removed';
  oldLine?: number;
  newLine?: number;
  content: string;
}

// 彩色渲染
function renderDiff(diff: DiffFile[], maxWidth: number): string {
  // ─ 行 → C.error (红色)
  // + 行 → C.success (绿色)
  //   行 → C.dim (灰色)
  // 文件头 → C.accent + 分隔线
}
```

### 集成点

1. **repl.ts `/diff` 命令** — `extractFileBlocks` 后生成 diff，使用 `renderDiff` 输出
2. **index.ts `ic d <task-id>`** — 同样使用 `renderDiff`
3. **report/generator.ts** — report diff 区块使用 `renderDiff`

### 验收

```
测试: + 行绿色，- 行红色，空格行灰色
测试: 多文件 diff 正确分隔
测试: 空 diff 显示 "无变更"
测试: 中文文件名/内容正确着色
```
