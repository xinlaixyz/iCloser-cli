# S20.6 错误恢复指引

日期：2026-05-15
负责人：dev1（全部独立）
优先级：🟢 P2
估时：2h
依赖：S20.3

## 问题

当前错误输出只显示原始错误信息，用户不知道下一步该做什么。

## 实现

### 文件: `src/cli/repl.ts` (扩展)

```typescript
interface ErrorRecovery {
  errorType: 'compile' | 'lint' | 'test' | 'e2e' | 'gate' | 'network' | 'timeout';
  title: string;
  details: string[];
  suggestions: Suggestion[];
}

interface Suggestion {
  label: string;
  action: string;    // 触发的命令或操作
  primary: boolean;  // 主要推荐
}

function renderErrorRecovery(err: ErrorRecovery): string {
  // 框标题: ╭─ tsc 编译失败 (2 errors) ─╮
  // 错误详情: src/config.ts:6:7 error TS2304: ...
  // 分隔线
  // 建议操作: ● 主要推荐  ○ 备选方案
}

// 从原始错误文本中提取错误类型和详情
function analyzeError(output: string, stage: string): ErrorRecovery {
  // 匹配 tsc / eslint / vitest / e2e 等不同工具的输出模式
  // 提取文件名、行号、错误码、错误描述
}
```

### 集成点

- `printVerifyResult()` 失败分支 → 调用 `analyzeError` + `renderErrorRecovery`
- `printGateResult()` 阻塞分支 → 同上
- AI 调用失败 (网络/超时) → 针对性建议

### 验收

```
测试: tsc 编译失败 → 提取文件名+行号+错误码+建议
测试: 门禁阻塞 → 展示阻塞项+具体修复建议
测试: 网络超时 → 建议检查 key / 切换 provider
测试: 成功时不显示错误指引
```
