# Dev1 S11-S16: CLI 工具链 + 代码智能

日期：2026-05-14
负责人：dev1
状态：✅ 全部完成

## 已完成阶段（与 dev3 联合）

| 阶段 | 内容 | dev1 贡献 |
|------|------|-----------|
| S8 | tree-sitter AST TS/JS | ast-parser.ts 核心提取器、scanner 集成 |
| S9 | 多语言 AST | Go/Python/Java/Kotlin/Swift/SQL/ObjC 解析器代码（grammar ABI 修复 + 降级保护） |
| S10 | Web Search | — (dev3 独立完成) |
| S11 | 跨文件调用图 + 增量扫描 | `buildCrossFileCallGraph()`、`computeFingerprints()`、types 类型定义 |
| S12 | Agent CLI/REPL | AgentManager `simulateExecution`→真实 Provider、`orchestrate()` 编排方法、`/run` `/agents` 命令实装、14 个单元测试 |

## S11: 跨文件调用图 + 增量扫描（dev1 部分）

### 文件
- `src/types.ts` — `CrossFileCallEdge` 接口、`ProjectIndex.callGraph`、`fileFingerprints`
- `src/core/scanner.ts` — `buildCrossFileCallGraph()`、`loadPreviousFingerprints()`、`computeFingerprints()`
- `tests/scanner.test.ts` — 跨模块调用图 + 指纹持久化测试 (2 tests)

### 验收
```bash
npm run test -- scanner       # 5 tests (含 S11 2 tests)
node dist/index.js scan       # index.json 含 callGraph + fileFingerprints 字段
```

## S11: AgentManager 实装

### 文件
- `src/agent/manager.ts` — 完全重写执行层
  - `runAgent()` — 真实 `createProvider().chat()` 替代 `setTimeout` mock
  - `orchestrate()` — AI 拆解任务 → 并行子 Agent → 聚合结果
  - `pause/resume` + `AbortController` 中断
  - `waitForAgent()` — 轮询等待完成
  - 5 个 Agent 模板（新增 Task Executor / Orchestrator）
- `src/types.ts` — `AgentInstance` + `startedAt`/`completedAt`、`AgentResult` + `error`
- `src/cli/repl.ts` — `/run` `/agents` 接入真实 AgentManager
- `tests/agent-manager.test.ts` — 14 个单元测试（生命周期/通信/上下文/层级/并发）

### 验收
```bash
npm run test -- agent-manager    # 14/14
```

## S13-S16 (dev1): CLI 命令扩展

### 新增 CLI 命令

| 命令 | 文件 | 说明 |
|------|------|------|
| `ic start` | `src/index.ts` | 启动项目 dev server |
| `ic stop` | `src/index.ts` | 停止后台进程 |
| `ic search <p>` | `src/index.ts` | 代码搜索 (ripgrep) |
| `ic search <q> --web` | `src/index.ts` | 网络搜索 (DuckDuckGo) |
| `ic search <p> --json` | `src/index.ts` | 搜索 JSON 输出 |
| `ic loop` | `src/index.ts` | 三步循环 + 工具矩阵 |
| `ic loop --json` | `src/index.ts` | 循环状态 JSON |
| `ic intel <q> --json` | `src/index.ts` | 代码智能 JSON 输出 |
| `ic intel <s> --callers` | `src/index.ts` | 仅显示调用者 |

### REPL 新增

| 命令 | 文件 | 说明 |
|------|------|------|
| `/intel` `/code` | `src/cli/repl.ts` | 代码智能查询：符号定义、调用者、模块导出 |
| 自然语言路由 | `src/cli/repl.ts` | `谁调用了 X` / `X 在哪里定义` / `模块 X 的导出` |

### 验收
```bash
npm run build
npm run test               # 318 passed, 18 skipped
npm run smoke              # PASS
ic loop --json             # 3 steps, 5 tools
ic intel scanProject --json # symbols + callers
```

## dev1 文件修改清单（供 dev3 检查）

| 文件 | 改动类型 | 说明 |
|------|----------|------|
| `src/types.ts` | 新增字段 | `CrossFileCallEdge`、`callGraph`、`fileFingerprints`、`AgentResult.error`、`AgentInstance.startedAt/completedAt`、`Task.loopState` |
| `src/core/ast-parser.ts` | try-catch 保护 | Java/Kotlin/Swift/ObjC/SQL 解析器代码 + require 降级 |
| `src/core/scanner.ts` | 新增函数 | `buildCrossFileCallGraph`、`computeFingerprints`、`loadPreviousFingerprints` |
| `src/core/task-engine.ts` | 新增函数 | `setTaskLoopStep`、`advanceTaskLoopState`、`completeTaskLoop` |
| `src/core/task-loop.ts` | 新建 | 三步循环 × 五大工具矩阵 |
| `src/core/execution-chain.ts` | 新建 | 10 阶段自动执行链 |
| `src/core/autopilot*.ts` | 新建 | autopilot/repair/rollback/verify/router 子系统 |
| `src/core/autodoc.ts` | 新建 | 自动文档生成 |
| `src/core/autotest.ts` | 新建 | 自动测试生成 |
| `src/agent/manager.ts` | 重写 | 真实 Provider 执行 + 编排 |
| `src/cli/repl.ts` | 多处新增 | loop-panel、intel、Agent、intervention routing |
| `src/cli/loop-panel.ts` | 新建 | 循环状态面板 |
| `src/cli/output.ts` | 新增函数 | `printToolDegradationNotice` |
| `src/index.ts` | 多处新增 | start/stop/search/loop CLI、executeTask loop hooks、intel |
| `tests/agent-manager.test.ts` | 新建 | 14 tests |
| `tests/scanner.test.ts` | 新增 tests | S11 调用图 + 指纹测试 |
| `tests/ast-parser.test.ts` | 新增 tests | Go/Py/Java/Kotlin/Swift/SQL (dev3+dev1) |
| `scripts/multilang-smoke.mjs` | 新建 | dev3 |
| `scripts/loop-tool-smoke.mjs` | 新建 | dev3 |

## 与 dev3 S13-S16 无冲突确认

| dev3 S13-S16 | 涉及文件 | dev1 已接触？ | 冲突？ |
|-------------|---------|-------------|--------|
| S13 Task→Agent 桥接 | `task-engine.ts`, `index.ts` | `executeTask` loop hooks 已加 | ✅ 无 — 不同代码区域 |
| S14 Agent Sandbox | `security.ts`, `manager.ts` | 未接触 security.ts | ✅ 无 |
| S15 Agent→Report | `report/generator.ts` | 未接触 | ✅ 无 |
| S16 文档完善 | `doc/` | 本文档 | ✅ 无 |
