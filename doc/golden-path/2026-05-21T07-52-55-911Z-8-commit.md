# G1 Real Provider 产物 8 — Commit / PR

## Commit Draft
```

Commit Draft
  docs: update project documentation

docs: update project documentation
变更摘要：
- index.js
- index.test.js
- AGENTS.md
验证建议：
- npx tsc --noEmit
- npm run lint
- npm test
Diff 摘要：
变更文件：3 个
Diff 行数：105

关键片段：
diff --git a/index.js b/index.js
index 7d3160b..73c93b9 100644
--- a/index.js
+++ b/index.js
@@ -1,3 +1,37 @@
-// Simple math utilities
-export function add(a, b) { return a + b; }
-export function multiply(a, b) { return a * b; }
+/**
+ * Validates that a value is a finite number.
+ * @param {*} value - The value to check.
+ * @param {string} name - The parameter name for error messages.
+ * @throws {TypeError} If the value is not a finite number.
+ */
+function validateNumber(value, name) {
+  if (typeof value !== 'number' || !isFinite(value)) {
+    throw new TypeError(`Argument '${name}' must be a finite number`);
+  }
+}
+
+/**
+ * Adds two finite numbers.
+ * @param {number} a - The first number.
+ * @param {number} b - The second number.
+ * @returns {number} The sum of a and b.
+ * @throws {TypeError} If either argument is not a finite number.
+ */
+export function add(a, b) {
+  validateNumber(a, 'a');
+  validateNumber(b, 'b');
+  return a + b;
+}
+
+/**
+ * Multiplies two finite numbers.
+ * @param {number} a - The first number.
+ * @param {number} b - The second number.
+ * @returns {number} The product of a and b.
+ * @throws {TypeError} If either argument is not a finite number.
+ */
+export function multiply(a, b) {
+  validateNumber(a, 'a');
+  validateNumber(b, 'b');
+  return a * b;
+}
diff --git a/index.test.js b/index.test.js
index 1bd5379..05dfc6f 100644
--- a/index.test.js
+++ b/index.test.js
@@ -2,5 +2,53 @@ import { add, multiply } from './index.js';
 import assert from 'node:assert';
 import { test } from 'node:test';
 
-test('add', () => { assert.strictEqual(add(1, 2), 3); });
-test('multiply', () => { assert.strictEqual(multiply(3, 4), 12); });
+// Positive tests
+test('add with finite numbers', () => {
+  assert.strictEqual(add(1, 2), 3);
+  assert.strictEqual(add(-1, -2), -3);
+  assert.strictEqual(add(0, 0), 0);
+  assert.strictEqual(add(1.5, 2.5), 4);
+});
+
+test('multiply with finite numbers', () => {
+  assert.strictEqual(multiply(3, 4), 12);
+  assert.strictEqual(multiply(-3, 4), -12);
+  assert.strictEqual(multiply(0, 5), 0);
+  assert.strictEqual(multiply(2.5, 2), 5);
+});
+
+// Negative tests (throws TypeError)
+test('add throws TypeError for non-number', () => {
+  assert.throws(() => add('1', 2), { name: 'TypeError', message: /Argument 'a' must be a finite number/ });
+  assert.throws(() => add(1, '2'), { name: 'TypeError', message: /Argument 'b' must be a finite number/ });
+  assert.throws(() => add(null, 2), TypeError);
+  assert.throws(() => add(undefined, 2), TypeError);
+  assert.throws(() => add(true, 2), TypeError);
+});
+
+test('add throws TypeError for NaN', () => {
[✓] 提交草稿已生成

```

## PR Draft
```

PR Draft
  Title: feat: 为 math 模块的 add 和 multiply 函数添加参数校验：参数必须为有限 Number，否则抛出 TypeError
  Base:  main
  Head:  master

## Summary
- index.js
- index.test.js
- AGENTS.md
## Verification
- [ ] npm run lint
- [ ] npm test
- [ ] npm run smoke:tools
## Commit range
(no local commits ahead of base, or base branch unavailable)

```