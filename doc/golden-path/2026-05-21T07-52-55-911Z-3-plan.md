# G1 Real Provider 产物 3 — AI 计划

## Provider 计划
1. Add a validateNumber helper to check finite number.
2. Modify add and multiply to call validateNumber before computing.
3. Add JSDoc comments to add and multiply.
4. Write positive tests for finite numbers.
5. Write negative tests for non-numbers, NaN, and Infinity.

## Provider 原始响应摘要
```
{
  "plan": [
    "Add a validateNumber helper to check finite number.",
    "Modify add and multiply to call validateNumber before computing.",
    "Add JSDoc comments to add and multiply.",
    "Write positive tests for finite numbers.",
    "Write negative tests for non-numbers, NaN, and Infinity."
  ],
  "changes": [
    {
      "file": "index.js",
      "content": "/**\n * Validates that a value is a finite number.\n * @param {*} value - The value to check.\n * @param {string} name - The parameter name for error messages.\n * @throws {TypeError} If the value is not a finite number.\n */\nfunction validateNumber(value, name) {\n  if (typeof value !== 'number' || !isFinite(value)) {\n    throw new TypeError(`Argument '${name}' must be a finite number`);\n  }\n}\n\n/**\n * Adds two finite numbers.\n * @param {number} a - The first number.\n * @param {number} b - The second number.\n * @returns {number} The sum of a and b.\n * @throws {TypeError} If either argument is not a finite number.\n */\nexport function add(a, b) {\n  validateNumber(a, 'a');\n  validateNumber(b, 'b');\n  return a + b;\n}\n\n/**\n * Multiplies two finite numbers.\n * @param {number} a - The first number.\n * @param {number} b - The second number.\n * @returns {number} The product of a and b.\n * @throws {TypeError} If either argument is not a finite number.\n */\nexport function multiply(a, b) {\n  validateNumber(a, 'a');\n  validateNumber(b, 'b');\n  return a * b;\n}\n"
    },
    {
      "file": "index.test.js",
      "content": "import { add, multiply } from './index.js';\nimport assert from 'node:assert';\nimport { test } from 'node:test';\n\n// Positive tests\ntest('add with finite numbers', () => {\n  assert.strictEqual(add(1, 2), 3);\n  assert.strictEqual(add(-1, -2), -3);\n  assert.strictEqual(add(0, 0), 0);\n  assert.strictEqual(add(1.5, 2.5), 4);\n});\n\ntest('multiply with finite numbers', () => {\n  assert.strictEqual(multiply(3, 4), 12);\n  assert.strictEqual(multiply(-3, 4), -12);\n  assert.strictEqual(multiply(0, 5), 0);\n  assert.strictEqual(multiply(2.5, 2), 5);\n});\n\n// Negative tests (throws TypeError)\ntest('add throws TypeError for non-number', () => {\n  assert.throws(() => add('1', 2), { name: 'TypeError', message: /Argument 'a' must be a finite number/ });\n  assert.throws(() => add(1, '2'), { name: 'TypeError', message: /Argument 'b' must be a finite number/ });\n  assert.throws(() => add(null, 2), TypeError);\n  assert.throws(() => add(undefined, 2), TypeError);\n  assert.throws(() => add(true, 2), TypeError);\n});\n\ntest('add throws TypeError for NaN', () => {\n  assert.throws(() => add(NaN, 2), TypeError);\n  assert.throws(() => add(1, NaN), TypeError);\n});\n\ntest('add throws TypeError for Infinity', () => {\n  assert.throws(() => add(Infinity, 2), TypeError);\n  assert.throws(() => add(1, -Infinity), TypeError);\n});\n\ntest('multiply throws TypeError for non-number', () => {\n  assert.throws(() => multiply('3', 4), { name: 'TypeError', message: /Argument 'a' must be a finite number/ });\n  assert.throws(() => multiply(3, '4'), { name: 'TypeError', message: /Argument 'b' must be a finite number/ });\n  assert.throws(() => multiply(null, 4), TypeError);\n  assert.throws(() => multiply(undefined, 4), TypeError);\n});\n\ntest('multiply throws TypeError for NaN', () => {\n  assert.throws(() => multiply(NaN, 4), TypeError);\n  assert.throws(() => multiply(3, NaN), TypeError);\n});\n\ntest('multiply throws TypeError for Infinity', () => {\n  assert.throws(() => multiply(Infinity, 4), TypeError);\n  assert.throws(() => multiply(3, -Infinity), TypeError);\n});\n"
    }
  ],
  "notes": [
    "Added validateNumber helper to enforce finite number check.",
    "Updated add and multiply signatures with JSDoc and checks.",
    "Provided positive tests for basic arithmetic cases.",
    "Provided negative tests covering non-numbers, NaN, Infinity, and edge values."
  ]
}
```