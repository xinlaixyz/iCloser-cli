# G1 Real Provider 产物 3 — AI 计划

## Provider 计划
1. Add input validation helper to check finite number arguments
2. Update add() and multiply() to validate inputs and throw TypeError for invalid inputs
3. Add JSDoc comments to both functions
4. Add positive and negative tests using node:test and node:assert

## Provider 原始响应摘要
```
{
  "plan": [
    "Add input validation helper to check finite number arguments",
    "Update add() and multiply() to validate inputs and throw TypeError for invalid inputs",
    "Add JSDoc comments to both functions",
    "Add positive and negative tests using node:test and node:assert"
  ],
  "changes": [
    {
      "file": "index.js",
      "content": "/**\n * Validates that a value is a finite Number.\n * @param {*} value - The value to validate.\n * @param {string} name - The argument name for error messages.\n * @throws {TypeError} If the value is not a finite Number.\n */\nfunction checkFinite(value, name) {\n  if (typeof value !== 'number' || !Number.isFinite(value)) {\n    throw new TypeError(`Argument '${name}' must be a finite number`);\n  }\n}\n\n/**\n * Adds two finite numbers.\n * @param {number} a - A finite number.\n * @param {number} b - A finite number.\n * @returns {number} The sum of a and b.\n * @throws {TypeError} If either argument is not a finite number.\n */\nexport function add(a, b) {\n  checkFinite(a, 'a');\n  checkFinite(b, 'b');\n  return a + b;\n}\n\n/**\n * Multiplies two finite numbers.\n * @param {number} a - A finite number.\n * @param {number} b - A finite number.\n * @returns {number} The product of a and b.\n * @throws {TypeError} If either argument is not a finite number.\n */\nexport function multiply(a, b) {\n  checkFinite(a, 'a');\n  checkFinite(b, 'b');\n  return a * b;\n}\n"
    },
    {
      "file": "index.test.js",
      "content": "import { add, multiply } from './index.js';\nimport assert from 'node:assert';\nimport { test } from 'node:test';\n\n// Positive tests\ntest('add with positive integers', () => {\n  assert.strictEqual(add(1, 2), 3);\n});\n\ntest('add with negative numbers', () => {\n  assert.strictEqual(add(-1, -1), -2);\n});\n\ntest('add with zero', () => {\n  assert.strictEqual(add(0, 5), 5);\n});\n\ntest('multiply with positive numbers', () => {\n  assert.strictEqual(multiply(3, 4), 12);\n});\n\ntest('multiply with zero', () => {\n  assert.strictEqual(multiply(0, 100), 0);\n});\n\ntest('multiply with negative numbers', () => {\n  assert.strictEqual(multiply(-2, 3), -6);\n});\n\n// Negative tests - add\nconst invalidAddCases = [\n  { args: ['a', 1], desc: 'first arg is string' },\n  { args: [1, null], desc: 'second arg is null' },\n  { args: [NaN, 1], desc: 'first arg is NaN' },\n  { args: [1, NaN], desc: 'second arg is NaN' },\n  { args: [Infinity, 2], desc: 'Infinity' },\n  { args: [-Infinity, 2], desc: '-Infinity' },\n];\n\nfor (const { args, desc } of invalidAddCases) {\n  test(`add throws TypeError when ${desc}`, () => {\n    assert.throws(() => add(...args), TypeError);\n  });\n}\n\n// Negative tests - multiply\nconst invalidMulCases = [\n  { args: [undefined, 2], desc: 'first arg undefined' },\n  { args: [2, '3'], desc: 'second arg string' },\n  { args: [NaN, 2], desc: 'NaN' },\n  { args: [2, Infinity], desc: 'Infinity' },\n];\n\nfor (const { args, desc } of invalidMulCases) {\n  test(`multiply throws TypeError when ${desc}`, () => {\n    assert.throws(() => multiply(...args), TypeError);\n  });\n}\n"
    }
  ],
  "notes": [
    "Added checkFinite helper to centralize input validation",
    "All validation errors are TypeError with descriptive messages",
    "Test suite covers positive cases and throws for non-number, NaN, Infinity arguments"
  ]
}
```