# AgentCode — UI/UX 设计文档

版本：0.4.0
日期：2026-05-20

> 2026-05-21 更新：新增 Grand Terminal Console 启动首屏方案与 AI 输出交互节奏要求。执行文档见 [`doc/tasks/2026-05-21-grand-terminal-ui-plan.md`](../doc/tasks/2026-05-21-grand-terminal-ui-plan.md)。
>
> 2026-05-22 更新：启动首屏进入实现基线。默认首屏不再强行展示终端 logo，避免低保真字符图破坏专业感；首屏排版改为干净的工程工作台入口；Quick start 改为三行短句，80/110 columns 下必须不越界。
>
> 2026-05-22 追加：任务驾驶舱 P0-P2 升级进入执行。目标是把 REPL 从工具流水账升级为 AI 工作指挥系统，执行文档见 [`doc/tasks/2026-05-22-mission-cockpit-p0-p2-execution.md`](../doc/tasks/2026-05-22-mission-cockpit-p0-p2-execution.md)。

## 设计原则

```
清晰度 > 装饰性    信息层级清楚，不乱
反馈感 > 沉默       每个动作都有响应，等待有进度
可达性 > 记忆       常用操作一键可达，减少输入
容错性 > 严格       错误有指引，操作可撤销
大气感 > 堆料       品牌首屏要像工程指挥台，不像脚本欢迎页
节奏感 > 倾倒       AI 输出分阶段推进，不一次性吐出大段内容
```

---

## 0. Grand Terminal Console 启动首屏

### 0.1 品牌要求

REPL 启动首屏必须固定使用产品品牌：

```text
i C l o s e r   Agent Shell
Terminal AI Engineering Assistant
```

启动屏目标不是“炫”，而是大气、稳、专业。它应像本地工程指挥台，能够传达：当前项目、AI Provider、长期记忆、上下文预算和可执行能力都已经就绪。

### 0.2 视觉方向

风格名：**Grand Terminal Console**

```text
黑底银灰 / 青绿状态 / 紫色点缀 / FC 像素加密朋克 / 工程终端 / 克制留白
```

规则：

- 不使用大面积紫色边框。
- 不使用无信息价值的右侧 ASCII 装饰。
- 不使用 logo 方框、卡片边框或占位框。
- 默认首屏不展示低保真字符 logo。
- 宽度优先 88-96 columns，低于 80 columns 自动紧凑。

### 0.3 首屏目标布局

```text
╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║   i C l o s e r   Agent Shell                                              ║
║   Terminal AI Engineering Assistant                                          ║
║                                                                              ║
║   PROJECT     Polymarket                PROVIDER    deepseek / deepseek-v4   ║
║   WORKSPACE   D:\temp\Codex\Polymarket   MEMORY      12 rules · 3 relevant   ║
║   STACK       Android · Gradle           CONTEXT     5.9K / 100K             ║
║                                                                              ║
║   Ready for engineering work: scan · edit · test · launch · explain          ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

  Session restored   15 messages · last topic: 启动项目

  /help   /scan   /diff   /mem   /start   /clear
```

### 0.4 Logo 分级

| 终端能力 | 展示方式 |
| --- | --- |
| 支持图片协议 | 显示无边框 FC 像素加密朋克 logo |
| 不支持图片协议 | 显示文本/Unicode 徽记 |
| 宽度不足 | 隐藏 logo，只保留品牌和状态 |

### 0.4.1 当前实现基线

当前 REPL 默认启动页不展示 logo，只展示品牌名与工程状态。

来源要求：

- Logo 母版以用户提供的 `iCloser-LOGOvector.svg` 为准。
- 默认首屏不得使用低保真字符 logo，因为半块像素锯齿明显，Braille 细节过密，轮廓图也无法稳定表达原始品牌。
- Logo 只在高保真图片协议、`/logo`、README 或品牌页中展示。
- 若未来要恢复默认首屏 logo，必须先通过人工截图验收，不能只靠宽度测试通过。

排版要求：

- 主框最大宽度控制在约 96 columns，避免全屏横向铺满。
- 宽屏时采用双列状态信息工作台布局。
- 窄屏时只保留品牌、Provider、Workspace 和 Context。
- `Quick start` 必须拆成多行，不允许中文长句冲出边框。
- Quick start 输入若已经带编号，渲染层必须去重，不能出现 `1  1`。

当前首屏信息层级：

```text
i C l o s e r   Agent Shell
Terminal AI Engineering Assistant

PROJECT / PROVIDER
WORKSPACE / MEMORY
STACK / CONTEXT
FLOW / EVIDENCE / CONTROL

Ready: scan · edit · test · launch · explain
Quick start
  1  直接输入需求
  2  /scan 扫描项目
  3  /help 查看所有命令和快捷键
```

### 0.5 首屏验收

- 第一眼能识别 `i C l o s e r Agent Shell`。
- 默认首屏不展示低保真 logo。
- 不再重复输出 Project/AI 状态栏。
- `Session restored` 不再以突兀小卡片显示。
- Windows Terminal、PowerShell、macOS Terminal、iTerm2 下不乱码、不错位。
- 80 columns 与 110 columns 下，所有首屏文本行都不能越界。
- Quick start 不允许单行塞入多个中文长句。
- Quick start 不允许出现重复编号。

---

## 0.6 AI 输出交互节奏

### 问题

当前 AI 内容容易一下子输出很多，用户看不到“它正在做什么”。这会削弱 Claude Code 替代品体验：缺少阶段感、缺少工具过程、缺少可中断的陪跑感。

### 目标

AI 输出从“结果倾倒”改为“任务陪跑”：

```text
◇ 启动项目

  ● 理解启动需求
  ● 识别 Android / Gradle 项目
  ● 检查 SDK、ADB 与模拟器
  ● 构建并安装 APK
  ● 启动 MainActivity

Tools
  ✓ read_file      app/build.gradle.kts          4.3K
  ✓ read_file      gradle/libs.versions.toml     6.4K
  ✓ run_command    adb devices                   emulator-5554 device
  ✓ run_command    gradlew installDebug          BUILD SUCCESSFUL

Result
  应用已启动，MainActivity 位于前台。
```

### 输出规则

- 任务开始先显示 3-6 个阶段。
- 任务开始必须显示本轮工具/搜索预算，让用户知道 AI 会如何控制工具数量和上下文。
- 工具调用前显示目标，工具完成后只显示一行摘要。
- 大段回答按 `Summary / Evidence / Next` 组织。
- 超过 12 行的日志或解释默认折叠。
- 重复读取文件显示 `reused from cache`，不再刷 warning。
- 输出中保持 `Ctrl+C 中断` 可见。
- 任务完成必须给出 `Result` 或 `Next`，不能只停在工具日志。
- 任务完成必须展示关键证据来源，网页/研究类任务优先显示来源域名，不能只显示工具名称。

### 0.6.1 任务驾驶舱

任务驾驶舱是 REPL 面向普通用户的主视图。它的职责不是装饰，而是把 AI 的工程闭环翻译成人能理解的执行路径。

开始面板必须包含：

```text
任务    用户原始需求
类型    代码交付 / 投资市场研究 / 网页访问 / 项目启动 / 长期记忆
目标    本轮要交付什么
AI      provider / model
记忆    是否注入项目规则和相关历史
目录    当前工作目录
预算    工具次数、搜索次数、证据压缩或文件读取策略
执行顺序  理解 → 取证 → 交付 → 验证
确认点    是否会写文件、运行命令、提交或发布
```

结果面板必须包含：

```text
理解    是否识别用户目标
取证    调用了哪些工具
交付    已生成报告 / 已生成补丁 / 已启动项目
验证    基于证据核对 / 命令运行证据 / 等待验证
记忆    是否使用任务记忆
证据    证据数量、工具次数、AI 轮数、token
来源    关键 URL 域名或本地文件
回答    最终回答行数
下一步  可执行恢复或继续动作
```

投资、市场、网页研究类任务必须避免代码话术，例如 `本轮没有代码补丁`、`等待写入后验证`。这类任务的验证标准是“是否有成功工具证据支撑结论”。

---

## 1. 输入系统

### 1.1 双线输入框

输入区域以 Unicode 双线框围住，视觉上区分「输入区」和「对话区」。

**默认状态 (单行):**

```
  ╭─ 输入 ───────────────────────────────────────────╮
  │                                                  │
  │  用户输入内容                                     │
  │  █                                               │
  │                                                  │
  ╰─ Enter 发送  │  Shift+Enter 换行  │  ↑↓ 历史 ────╯
```

**多行输入 (Shift+Enter 换行后):**

```
  ╭─ 输入 ───────────────────────────────────────────╮
  │                                                  │
  │  帮我把数据库连接改成连接池模式                    │
  │  具体要求：                                       │
  │  1. 最小连接数 5，最大 20                         │
  │  2. 超时 30 秒                                   │
  │  █                                               │
  │                                                  │
  ╰─ Enter 发送  │  Shift+Enter 换行  │  ↑↓ 历史 ────╯
```

**规则:**
- `Enter` → 发送当前输入内容
- `Shift+Enter` → 插入换行符 `\n`
- 输入框最小高度 3 行，最大高度 6 行 (超出滚动，顶部显示 `↑ 还有 N 行`)
- 发送后框自动收起到单行

### 1.2 历史导航

- `↑` / `↓` 在输入框内切换历史记录
- `Ctrl+R` 打开历史搜索面板，实时过滤

### 1.3 输入校验

- 输入 `/` 后实时提示命令自动补全
- 未知命令灰色斜体显示

---

## 2. 变更预览 (Diff 着色)

### 2.1 代码变更

```
  ╭─ 变更预览 ───────────────────────────────────────╮
  │                                                  │
  │  src/config.ts                                   │
  │  ───────────────────────────────────────────     │
  │   1  import { Pool } from 'pg-pool';             │
  │ ─ 2  const db = { single: true }                  │
  │ + 2  const pool = new Pool({ min: 5, max: 20 })   │
  │                                                  │
  ╰──────────────────────────────────────────────────╯
```

**着色规则:**
- `─` 删除行：红底/红色前景，行首 `─` 标记
- `+` 新增行：绿底/绿色前景，行首 `+` 标记
- 未变更行：灰色，不带行首标记

### 2.2 文档变更

同样规则适用于 Markdown 文档：

```
  ─ ## 旧标题        (红色)
  + ## 新标题        (绿色)
  ─ 已废弃的说明      (红色)
  + 新增的说明        (绿色)
```

### 2.3 全局启用

- REPL `/diff` 命令：预览当前 pending files
- CLI `ic d <task-id>` 命令：查看任务 diff
- 任务报告中的 diff 区块

---

## 3. 输出消毒

### 3.1 过滤规则

所有进入 `process.stdout` 的输出经过 `sanitizeOutput()` 过滤器：

| 字符范围 | 处理方式 |
|---------|---------|
| ASCII 0-8 | 丢弃 |
| ASCII 11-12 | 丢弃 |
| ASCII 14-31 | 丢弃 |
| \n (10), \r (13), \t (9) | 保留 |
| 未闭合 ANSI CSI 序列 | 丢弃序列 |
| 超长行 (>1000字符) | 截断 + `…(截断)` |
| � 替换字符 | 丢弃 |
| 连续 3+ 相同非 ASCII 字符 | 替换为 `�` |

### 3.2 集成点

- `printChatChunk()` — 流式 AI 输出逐行输出
- `process.stdout.write` — 全局兜底包装
- 启动时打印 "输出过滤器已启用"

---

## 4. 等待体验

### 4.1 三阶段反馈

**阶段 1 — 连接 (0-1s):**
```
  ◉ 正在连接 DeepSeek API...
```

**阶段 2 — 等待首字节 (1-15s):**
```
  ◉ AI 分析中 [4.7s]  ██████░░░░░░░░░░░░
```

- 计时器每秒更新 `[N.Ns]`
- 脉冲动画：`◉` `◔` `◑` `◕` 循环，每 80ms 切换
- 超过 10s 显示提示：`复杂任务可能需 10-30 秒，Ctrl+C 可中断`

**阶段 3 — 流式输出:**
```
  ◉ 输出中 [8.1s]  已接收 847 tokens

  好的，我来帮你修改数据库配置。首先分析...
  █  (内容持续追加)
```

### 4.2 Token 计数

从 AI 响应中实时提取 `x-tokens` 或从 stream chunk 估算，每秒刷新一次。

### 4.3 提前呈现

- 不等到完整 code block 才渲染
- 逐行从 stream buffer 中取出，即时打印
- 代码块识别到 ``` 开启/关闭时分别渲染边框

---

## 5. 底部固定操作面板

### 5.1 空闲状态

底部常驻快捷命令栏：

```
  ╭─ 操作 ───────────────────────────────────────╮
  │  help  帮助  │  scan  扫描  │  status  状态    │
  │  write 写入  │  diff  预览  │  clear  清屏     │
  │  Ctrl+P 命令面板                              │
  ╰───────────────────────────────────────────────╯
```

按键规则：输入框为空时，按对应字母直接触发操作。有输入内容时字母进入输入框，不触发操作。

### 5.2 有待写入文件时

```
  ╭─ 待处理文件 (3) ───────────────────╮
  │ ● src/config.ts             +12 行  │
  │ ● src/types.ts               +5 行  │
  │ ● docs/README.md             +3 行  │
  ├─ 操作 ──────────────────────────────┤
  │ [w] 写入全部  [d] 预览变更  [x] 取消│
  ╰─────────────────────────────────────╯
```

- `w` → 写入全部文件
- `d` → 打开 diff 预览
- `1/2/3` → 选择/取消单个文件
- `x` → 清空 pending files

### 5.3 AI 执行中

```
  ╭─ 状态 ────────────────────────────────────────╮
  │ ◉ AI 执行中 [12.3s]  任务: 修改数据库配置       │
  │ Ctrl+C 中断                                     │
  ╰─────────────────────────────────────────────────╯
```

### 5.4 验证完成

```
  ╭─ 验证通过 ──────────────────────────────────────╮
  │ ● tsc 编译    ✓ 0 errors                       │
  │ ● eslint      ✓ 0 warnings                     │
  │ ● vitest      ✓ 18/18 passed                   │
  ├─ 操作 ──────────────────────────────────────────┤
  │ [c] 提交  [u] 撤销  [r] 查看报告                │
  ╰─────────────────────────────────────────────────╯
```

### 5.5 验证失败

```
  ╭─ 验证失败 (2/4 通过) ──────────────────────────╮
  │ ● tsc 编译    ✓ 通过                           │
  │ ● eslint      ✗ 2 warnings                     │
  │ ● vitest      ✓ 通过                           │
  │ ● e2e         ✗ 超时                           │
  ├─ 操作 ──────────────────────────────────────────┤
  │ [f] AI 自动修复  [u] 撤销变更  [i] 忽略继续     │
  ╰─────────────────────────────────────────────────╯
```

### 5.6 技术实现

- 利用现有 `TuiScreen` 类 (src/cli/tui.ts) 扩展
- 底部面板控制序列 `\x1b[s` 保存光标，渲染面板，`\x1b[u` 恢复
- 面板行数动态计算：内容区 = 终端高度 - 面板高度 - 输入框高度
- SIGWINCH 信号监听窗口大小变化，自动重绘

---

## 6. 命令面板 (Ctrl+P)

```
  ╭─ 命令面板 ────────────────────────────────────────╮
  │ > sca                                              │
  │ ─────────────────────────────────────────────────── │
  │ /scan           扫描项目并更新索引                  │
  │ Ctrl+P / Esc 关闭                                   │
  ╰────────────────────────────────────────────────────╯
```

- 模糊匹配命令名和描述
- ↑↓ 导航，Enter 执行，Esc 关闭

---

## 7. 错误恢复指引

### 7.1 编译/测试失败时

```
  ╭─ tsc 编译失败 (2 errors) ──────────────────────────╮
  │                                                     │
  │  src/config.ts:6:7                                   │
  │  error TS2304: Cannot find name 'Pool'               │
  │                                                     │
  │  src/types.ts:12:15                                  │
  │  error TS2322: Type 'string' is not assignable       │
  │                                                     │
  ├─ 建议操作 ──────────────────────────────────────────┤
  │  ● 输入描述错误让 AI 修复                            │
  │  ○ 运行 /doctor 查看完整诊断                         │
  │  ○ 运行 /undo 撤销此次变更                           │
  ╰─────────────────────────────────────────────────────╯
```

### 7.2 门禁阻塞时

```
  ╭─ 门禁阻塞 ─────────────────────────────────────────╮
  │                                                    │
  │ ✗ 安全扫描 — 检测到 .env 文件包含密钥               │
  │ ✗ 报告 — 缺少任务推理说明                           │
  │                                                    │
  ├─ 建议操作 ─────────────────────────────────────────┤
  │  ● 将 .env 加入 .gitignore 并删除敏感值             │
  │  ● 运行 /report 生成完整报告                        │
  ╰────────────────────────────────────────────────────╯
```

---

## 8. 文件结构

本设计涉及以下源码文件：

| 文件 | 实际内容 |
|------|----------|
| `src/cli/tui.ts` | 底部面板 renderBottomPanel + DEFAULT_SHORTCUTS |
| `src/cli/theme.ts` | 终端 UI 设计系统 (颜色/框线/图标) |
| `src/cli/diff-renderer.ts` | 新建 — parseDiff/renderDiff 红绿渲染 |
| `src/cli/output.ts` | sanitizeOutput + enableOutputSanitizer + printErrorRecovery |
| `src/cli/repl.ts` | 输入框顶 + 面板快捷键 + 等待三阶段 + 命令面板 /? + 历史搜索 !query |
| `src/report/generator.ts` | diff 区域改用 diff-renderer |

---

## 9. 完成度 (2026-05-15 最终)

| 功能 | 完成度 | 说明 |
|------|--------|------|
| 输出消毒 (S20.1) | ✅ 100% | sanitizeOutput + stdout 全路径过滤 |
| 等待动画 (S20.2) | ✅ 100% | 等待:脉冲◉◔◑◕+进度条+计时 / 流式:首尾标记,内容干净 |
| 底部面板 (S20.3) | ✅ 90% | 4 状态 + h/s/w/d/c/q 快捷键直达 |
| Diff 着色 (S20.4) | ✅ 100% | ─红 +绿, REPL /diff + CLI ic d |
| 输入框 (S20.5) | ✅ 100% | ╭─╮框顶 + │◇输入行 + ╰─╯框底,Enter后完整闭合 |
| 多行/Shift+Enter | ❌ 0%   | raw mode 破坏 IME+ANSI,终端跨平台不可行 |
| 错误指引 (S20.6) | ✅ 100% | 编译/lint/test/e2e 失败时显示修复建议 |
| 命令面板 (S20.7) | ✅ 90% | `/?` 浏览25命令, `/? scan` 过滤搜索 |
| 历史搜索 (S20.8) | ✅ 90% | `!query` 搜索, `!1` `!2` 数字复用 |
| Tab 补全 (S20.9) | ✅ 100% | /write/diff 文件路径 + /config/apikey 深度子补全 |
| 验收 (S20.10) | ✅ 100% | 43 files / 427 tests |

**综合: 92%** (排除 1 项不可行, 加权平均)

---

## 10. 完成度总览 (2026-05-17)

| 功能 | 状态 | 说明 |
|------|------|------|
| 输出消毒 | 100% | stdout 全路径过滤 |
| 等待动画 | 100% | ◉◔◑◕ 脉冲 + 诚实计时 (无假进度条) |
| 状态栏 | 100% | 6 模式: 系统操作/文件待写/AI执行/确认待处理/任务阶段/空闲 |
| Diff 着色 | 100% | ─红 +绿 |
| 输入 | 100% | ◇ 纯文本 prompt, 提示独立打印 |
| 错误指引 | 100% | 分类建议: Key问题/模型问题/网络重试/内部错误 |
| 命令面板 | 90% | /? /p 浏览过滤, 大小写不敏感 |
| 历史搜索 | 100% | !query + !N 复用 + Tab 补全 (已修复) |
| Tab 补全 | 100% | 文件路径 + 深度子补全 + !N 历史 |
| 代码块 | 100% | ┌/│/└ ChatGPT 风格左边框 + 折叠提示 |
| 表格渲染 | 100% | ╭┬╮/├┼┤/╰┴╯ 边框 + CJK 自适应列宽 + 左中右对齐 |
| TLDR | 100% | >30 行首行预览 |
| 编排树 | 100% | ├─ └─ 子 Agent |
| 简洁/详细 | 100% | /brief /full 切换 |
| 快捷键 | 100% | y/n/h/s/d/c/w/q 直达 + /help 文档 |
| 启动面板 | 100% | 欢迎屏 + 首次指南合并为单一面板 |
| AI 启动分析 | 100% | 读配置→AI 判断 isServer/依赖/顺序/前置条件 |
| 工具防幻觉 | 100% | tools 传入 API + system prompt 禁止模拟标签 |
| 会话恢复 | 100% | 含最后问题预览 |
| 记忆展示 | 100% | /memory 显示最近 5 轮对话摘要 |
| 验收 | 100% | 65 files / 644 tests |

**综合: 98%**

### 2026-05-20 新增

| 功能 | 状态 | 说明 |
|------|------|------|
| 上下文预加载 | 100% | 分析意图 AI 首轮注入 30 个关键代码文件 + 项目记忆 |
| 深度分析路由 | 100% | 分析意图自动触发 deep scan + 80K token 预算 + 6 轮工具循环 |
| 对话历史延长 | 100% | 分析意图保留 20 条历史（原 6 条） |
| 记忆容量提升 | 100% | Recall 12 条 / 6K tokens（原 5 条 / 2K） |
| 表格 CJK 列宽修复 | 100% | 修复 `const excess` bug 导致首列过度裁剪 |
| 项目检测增强 | 100% | 13 种编程语言 + 6 种非代码分类（文档/配置/数据/IaC/空目录） |

### 不可行项

| 项 | 根因 |
|----|------|
| Shift+Enter | raw mode 破坏 CJK IME, 跨平台无解 |
| Ctrl+P / Ctrl+R | readline 拦截控制键 |
| 面板固定底部 | 终端无 position:fixed |

---

## 11. 最终架构 (2026-05-15 精简重构)

### 11.1 设计哲学

放弃与终端底层对抗，拥抱原生滚动体验。

| 旧方案问题 | 精简方案 |
|-----------|---------|
| `\x1b[NA` 光标移动不稳定 | 零 ANSI 操作，纯文本输出 |
| `╭─╮ ╰─╯` 框线堆叠 | 无框线，单行状态栏 |
| 面板 4 状态 6 键 | 状态栏一行，4 种自适应 |
| readline prompt 含框 | `◇ ` 纯文本 prompt |
| raw mode 破坏 IME | readline 原生支持中文 |
| 200+ 行渲染代码 | 60 行状态栏 + 核心功能 |

### 11.2 最终布局

```
  ... 对话内容自然滚动 ...                    ← console.log 输出
                                              ← 空白行
  ◇  用户输入                                 ← readline prompt
                                              ← 空白行
  ◉ AI 分析中 [3.2s]  ██████░░░░             ← 等待(脉冲动画,80ms刷新)
                                              ← 空白行
  ◉ AI 输出中...                              ← 流式开始(一行标记)
  AI 回复内容...                               ← stream, 无 \r 污染
  │ 表格 │ 完整 │ 干净 │                       ← markdown 正确渲染
  ✓ [15.3s]                                    ← 流式结束(一行标记)
                                              ← 空白行
  ─ /help /scan /diff /clear · 2.4K/128K ─    ← 状态栏(单行)
  ◇  █                                        ← 下一个 prompt
```

### 11.3 状态栏 4 模式

```
系统操作待批: ⚡ 启动项目 ─ [y] 允许 [n] 拒绝
有文件待写:   ▸ src/config.ts+12 docs/readme.md+5 ─ /write 写入 /diff 预览
AI 执行中:    ◉ AI 执行中... Ctrl+C 中断
空闲:         ─ /help /scan /diff /clear · 2.4K/128K ─
```

### 11.4 快捷键（单字母直达）

| 键 | 功能 | 条件 |
|----|------|------|
| `h` | 显示帮助 | 空闲 |
| `s` | 扫描项目 | 空闲 |
| `d` | 预览变更 | 空闲 |
| `c` | 清空对话 | 空闲 |
| `w` | 写入文件 | 有 pending files |
| `q` | 退出 | 空闲 |
| `y` | 批准系统操作 | 系统操作待批 |
| `n` | 拒绝系统操作 | 系统操作待批 |

### 11.5 核心保留功能

| 功能 | 状态 |
|------|------|
| 输出消毒 sanitizeOutput | 100% |
| 三阶段等待: 脉冲+进度条+计时 | 100% |
| Diff 红绿着色 | 100% |
| 代码块折叠 >30 行 | 100% |
| TLDR 长响应摘要 >30 行 | 100% |
| 表格渲染: 表头加粗+分隔线 | 100% |
| JSON 输出契约隐藏 | 100% |
| 错误恢复指引 | 100% |
| 命令面板 `/?` `!query` `!N` | 100% |
| 编排树形可视化 | 100% |
| 简洁/详细模式 `/brief` `/full` | 100% |

### 11.6 不可行项（终端硬件限制）

| 项 | 原因 |
|----|------|
| Shift+Enter 换行 | raw mode 破坏 CJK IME |
| 面板固定底部 | 无终端 position:fixed |
| Ctrl+P / Ctrl+R | readline 拦截控制键 |

### 不可行项

| 项 | 根因 |
|----|------|
| Shift+Enter 换行 | Node raw mode 在 Windows 破坏 CJK IME 组合输入 |
| 面板固定底部 | 终端无 position:fixed; alternate buffer 会隐藏对话历史 |
| Ctrl+P / Ctrl+R | readline 拦截控制键, 不支持重映射 |

| `src/index.ts` | CLI `ic d` 命令改用 diff-renderer |

---

## 12. 变更记录 (2026-05-16 ~ 2026-05-17)

### 启动流程
- **三层 UI 冲突修复**: 多服务发现框 + 系统权限确认面板 + ⚡ 状态行 → 合并为单一面板
- **AI 启动前分析**: `analyzeStartupPlan()` 读取 Makefile/package.json/README → AI 判断服务类型、启动顺序、前置条件
- **启动面板合并**: 欢迎屏 + 首次使用指南从两个独立框合并为一个统一面板，步骤嵌入框内
- **提示与输入分离**: `/help 命令 · /scan 扫描` 印在界面行，提示符保持干净 `◇ `

### 渲染引擎
- **表格渲染重写**: 从逐行等宽截断 → 缓冲整表后自适应列宽 + ╭┬╮/├┼┤/╰┴╯ 完整边框 + 左中右对齐
- **CJK 显示宽度**: `displayWidth(str)` 函数正确计算全角字符为 2 终端列，修复中文表格对齐
- **ChatGPT 风格代码块**: `┌ lang` / `│ code` / `└ N 行` 左边框，替代裸 ``` 标记
- **标题留空**: H1/H2 前自动空行

### 安全与可靠性
- **c 键清除加确认**: 按 c → 显示提示 → 按 y 确认（防止误触）/ `/clear` 同步加确认
- **文件写入守卫**: `handleInlineConfirm` 加入 `!activeChoicePanel` 防止静默写入
- **会话 Key 脱敏**: `saveSession` 正则匹配 `sk-ant-*/sk-or-*/dashscope-*` 掩码
- **CJS require() 修复**: `context.ts`/`tool-executor.ts` 的 `require()` 改为 ESM `import`
- **Ctrl+C 取消确认**: pending 状态时 Ctrl+C 取消确认并打印提示
- **退出进程超时**: `stopStartedProcess` 加 5 秒超时防挂起

### 体验优化 (30 项审计 → 21 项修复)
- **提示符**: 前几次交互显示 `/help` 提示
- **错误恢复**: 分类建议 (Key/模型/网络/内部错误)
- **会话记忆**: `/memory` 从计数 → 最近 5 轮对话摘要
- **会话恢复**: 提示含最后问题预览
- **等待指示器**: 去掉假进度条，只保留脉冲 + 时间
- **convPhase 状态**: 状态行显示"任务已创建/执行中/已完成"
- **Ctrl+C 提示**: 提示符显示 `[再次 Ctrl+C 退出]`
- **快捷键文档**: `/help` 底部显示 y/n/h/s/d/c/w/q
- **命令面板**: 大小写不敏感搜索
- **流式输入缓冲**: AI 执行中用户输入缓存，完成后自动处理
- **中断保存**: Ctrl+C 中断时部分 AI 响应保存到会话
- **退出进程列表**: 显示正在停止的后台进程
- **会话过期提示**: "X 小时前" 信息
- **配置损坏恢复**: 备份 + 通知 + 重置
- **代码折叠指示**: 触发时立即显示提示
- **离线模式通知**: 运行时静默切换到 mock 时一次性通知
- **欢迎画面精简**: 去掉 Platform/Node/Time
- **首次指南**: 添加示例和 `/help` 链接
- **网络重试**: 2→1→0s 倒计时动画
- **工具防幻觉**: tools 传入 DeepSeek API + system prompt 禁止模拟 XML 标签

### 工程
- **测试**: 新增 `dag-scheduler.test.ts` (8) + `code-writer.test.ts` (8) = 51 files / 498 tests
- **依赖**: 删除未使用的 `diff`, `glob`, `marked`, `yaml` + `@types/diff`
- **文档**: ARCHITECTURE.md / DEVELOPER_GUIDE.md 数字更新

### 第二轮修复 (2026-05-17) — 14 项

**高危 (3):**
- 验证管线重复推送修复: stage 失败→修复成功 push 3 次 → 只 push 1 次 (`verifier.ts:37-121`)
- `getTestFilePath` 路径修复: `tests/src/services/user.test.ts` → `tests/user.test.ts` (`code-writer.ts:80-84`)
- `.env.example` 空内容泄露真 `.env`: `||` → `!= null` 判断 (`startup-analysis.ts:77`)

**中危 (8):**
- 工具 JS 异常误判成功: 加 `startsWith('工具执行异常')` 检查 (`execution-engine.ts:203`)
- `npx` → 平台感知 `npx.cmd` (`code-writer.ts:426`)
- 验证循环丢弃 test 修复: 同步查 `result.tests` (`code-writer.ts:230`)
- `queue --watch` 按 q 立即退出 (`index.ts:656`)
- `.gate-tmp` 文件泄露: 全局追踪 + 最终清理 (`code-writer.ts:334+`)
- 文件锁 3 分钟超时: 子任务完成后 `clearTimeout` + 立即释放 (`agent/manager.ts:285`)
- `ic start` 加 `windowsHide: true` (`index.ts:1693`)
- `npx.cmd` 加 windowsNative 正则 (`tool-executor.ts:157`)

**低危 (3):**
- `powershell`/`cmd` 从 windowsNative 移除防绕过安检 (`tool-executor.ts:157`)
- `find` 在 WIN_CMD_MAP 重复定义删除 (`tool-executor.ts:101`)
